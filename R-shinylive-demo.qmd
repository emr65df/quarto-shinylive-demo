---
title: "r-shinylive-demo with some data!"
format:
  html:
    resources: 
      - shinylive-sw.js
filters:
  - shinylive
---

```{shinylive-r}
#| standalone: true
#| viewerHeight: 600
library(shiny)
library(bslib)

beaver_data <- datasets::beaver2
# Define UI for app that draws a histogram ----
ui <- page_sidebar(
  sidebar = sidebar(open = "open",
                    selectInput("dataframe_column",
                                label = "Choose Distribution",
                                choices = list("b",
                                               "n"),
                                selected = "b")
  ),
  plotOutput("plot", width=1100)
)

server <- function(input, output, session) {

  
  output$plot <- renderPlot({
    hist(beaver2$temp
  )
   
  }, res=140)
}

# Create Shiny app ----
shinyApp(ui = ui, server = server)
```